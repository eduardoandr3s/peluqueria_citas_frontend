// Entorno de PRODUCCION (usado por `ng build`). Cambia la URL por la del API
// desplegado (HTTPS obligatorio para Android) antes de publicar.
export const environment = {
  production: true,
  apiUrl: 'https://TU-API-EN-PRODUCCION.com/api',
};
