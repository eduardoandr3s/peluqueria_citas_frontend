// Entorno de DESARROLLO (usado por `ng serve`).
// El backend Spring Boot corre por defecto en el puerto 8080.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
};
