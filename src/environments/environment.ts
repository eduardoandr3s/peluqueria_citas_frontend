// Entorno de PRODUCCIÓN (usado por `ng build`).
// Cambia esta URL por la del API desplegado cuando publiques.
export const environment = {
  production: true,
  apiUrl: 'https://TU-API-EN-PRODUCCION.com/api',
};
